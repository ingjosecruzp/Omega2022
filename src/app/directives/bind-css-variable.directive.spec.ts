import { BindCssVariableDirective } from './bind-css-variable.directive';

describe('BindCssVariableDirective', () => {
  it('should create an instance', () => {
    const directive = new BindCssVariableDirective();
    expect(directive).toBeTruthy();
  });
});
